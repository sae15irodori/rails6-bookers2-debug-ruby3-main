 <% if book.favorited_by?(current_user) %><!--取得した投稿データと関連付けたfavoriteモデルのuser_idカラムにｶﾚﾝﾄﾕｰｻﾞｰのidがあれば-->
  <td>
    <%= link_to book_favorites_path(book), method: :delete, remote: true, data: {"turbolinks" => false} do %><!--remote:trueによって、HTMLではなく、javascriptのリクエストがfavoritesコントローラに送られる-->
      ♥<%= book.favorites.count %>
    <% end %>
  </td>
<% else %>
  <td>
    <%= link_to book_favorites_path(book.id), method: :post, remote: true,data: {"turbolinks" => false} do %>
      ♡<%= book.favorites.count %>
    <% end %>
  </td>
<% end %><!--if終了-->
